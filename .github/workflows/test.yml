name: Test

on:
push:
  branches: [ main ]
pull_request:
  branches: [ main ]


jobs:
  test-job:
    runs-on: slurm-runner-small
    steps:
    - name: Check wato-drive access
      run: |
        ls /mnt/
